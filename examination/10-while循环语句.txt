1. 以下叙述正确的是 [单选题] [必答题]
   ○ A) do-while语句构成的循环不能用其它语句构成的循环来代替。
   ○ B) do-while语句构成的循环只能用break语句退出。
   ○ C) 用do-while语句构成的循环,在while后的表达式为非零时结束循环。
    ○ D) 用do-while语句构成的循环,在while后的表达式为零时结束循环。

2. 有以下程序
main ( )
{ 
    int s = 0, a= 1 , n;
    scanf ("%d" , &n );
        do
        { 
            s += 1; a = a - 2;
         }
    while ( a != n );
    printf ( "%d\n", s);
} 
若要使程序的输出值为2，则应该从键盘给n输入的值是 [单选题] [必答题]
   ○ A) -1
    ○ B) -3
   ○ C) -5
   ○ D) 0
   
3. 有以下程序段
int n = 0, p;
do
{
    scanf ( "%d", &p );
    n++;
}
while ( p != 12345 && n < 3);
此处 do—while 循环的结束条件是 [单选题] [必答题]
   ○ A) P的值不等于12345并且n的值小于3
   ○ B) P的值等于12345并且n的值大于等于3
   ○ C) P的值不等于12345或者n的值小于3
    ○ D) P的值等于12345或者n的值大于等于3

4. 有以下程序段 int k = 0 while ( k = 1 )k++; while 循环执行的次数是 [单选题] [必答题]
    ○ A) 无限次
   ○ B) 有语法错，不能执行
   ○ C) 一次也不执行
   ○ D) 执行1次


5. 有如下程序 
main()
{ 
    int  x = 23;
    do
    { 
        printf ( "%d", x-- );   
    } while ( !x );
}
该程序的执行结果是 [单选题] [必答题]
   ○ A) 321
    ○ B) 23(正确答案)
   ○ C) 不输出任何内容
   ○ D) 陷入死循环

6. 以下程序的输出结果是
main( )
{ 
    int n = 4;
    while ( n-- )
    printf ( "%d", --n );
 } [单选题] [必答题]
    ○ A) 2 0
   ○ B) 3 1
   ○ C) 3 2 1
   ○ D) 2 1 0
重点：递增、递减运算符的使用，前++(- -)和后++(- - )的区别

7. 执行以下程序段时
x = -1; 
do
{
    x = x * x ;
} 
while ( ! x ); [单选题] [必答题]
    ○ A) 循环体将执行一次
   ○ B) 循环体将执行两次
   ○ C) 循环体将执行无限次
   ○ D) 系统将提示有语法错误

8. 执行以下程序后,输出的结果是
main ( ) 
{ 
    int y = 10;    
    do
    { 
        y - -;
    }while ( - - y);
	printf ( "%d\n", y - - );
} [单选题] [必答题]
   ○ A) -1
   ○ B) 1
   ○ C) 8
    ○ D) 0
9. 语句while(!E);中的条件!E等价于　。 [单选题] [必答题]
    ○ A)E == 0
   ○ B)E！=1
   ○ C)E！=0
   ○ D)~E

10. 在执行以下程序时,如果从键盘上输入,ABCdef<回车>,则输出为

# include <stdio.h>
main( )
{ 
    char ch;
    while ( (ch = getchar())  != '\n' )
    { 
    if ( ch >= 'A' && ch <= 'Z' )  
        ch = ch + 32;
    else if ( ch >= 'a'  && ch <= 'z' ) 
        ch = ch - 32;
    printf ( "%c" , ch );
    }
	printf ( "\n" );
} [单选题] [必答题]
   ○ A) ABCdef
    ○ B) abcDEF
   ○ C) abc
   ○ D) DEF

11. 设有以下程序:
main( )
{ 
    int n1, n2;
    scanf ( "%d", &n2 );
    while ( n2 != 0 )
    { 
        n1 = n2 % 10;
        n2 = n2 / 10;
        printf ( "%d" , n1 );
    }
}
程序运行后，如果从键盘上输入1298；则输出结果为___8921____。 [填空题] [必答题]
  _________________________________
  
12. 下面程序的输出是 __________。
main( )
{ 
    int x = 2;
    while ( x -- );
    printf ( "%d\n",  x ) ;
} [填空题] [必答题]

  _________-1________________________

13. 设有如下程序段:
int i = 0, sum = 1;
do
{ 
    sum += i++ ; 
}
while ( i < 6 );
printf ( "%d\n", sum );
上述程序段的输出结果是__16______ [填空题] [必答题]

  _________________________________

14. 有以下程序：
# include main( )
{ 
    char c;
    while ( ( c = getchar( ) ) != '?' )  putchar ( --c ) ;
}
程序运行时，如果从键盘输入：Y？N？<回车>，则输出结果为___X____。 [填空题] [必答题]

  _________________________________
重点，getchar与putchar函数的使用
15. 若输入字符串：abcde<回车>,则以下while循环体将执行________次。
While ( ( ch = getchar ( ) ) == 'e')  printf ( "*" ); [填空题] [必答题]

  ____________0_____________________(答案：0)  








